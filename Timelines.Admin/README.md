# Timelines.Admin
Blazor Server admin/support app.

Responsibilities:
- Role-based access for support staff
- User management (as allowed by External ID/B2C approach)
- CRUD views for database entities
- Support tooling (search, inspection)